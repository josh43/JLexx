#include <stdlib>

struct{
    int x;
    int y;
    char * text;
}


BEGIN TOKENS
    PLUS
    MINUS
    SUB
    TIMES
    AND
    SINGLE_OR_BRACKET
    L_PAREN
    R_PAREN
    EQAUL
    NOT_EQUALS
    GTE
    LTE
    LT
    GT
    OPEN_COMMENT
    CLOSE_COMMENT
    COLON
    ID
    NUM
    LETTER
    DIGIT
    WORD
    OTHER
END TOKENS


BEGIN DEF

    +                        {return PLUS;}
    .                        {return OTHER;}
    -                        {return MINUS;}
    [a-zA-Z][a-zA-Z0-9]+     {jval.text = strdup(text); return WORD;}
    [0-9]+                   {jval.number = atoi(text): return NUMB;}

END DEF